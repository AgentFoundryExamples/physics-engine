# Physics Engine Environment Configuration

# Plugin Search Paths
# ---------------------
# Colon-separated paths on Unix/Linux/macOS, semicolon-separated on Windows
# The engine will search these directories for plugin libraries
# 
# Note: Dynamic plugin loading is not currently implemented. This variable
# is parsed but no dynamic loading occurs. Use static registration via
# PluginRegistry::register() for now.
#
# Example (Unix):
# PHYSICS_ENGINE_PLUGIN_PATH=/usr/local/lib/physics-plugins:/home/user/custom-plugins
#
# Example (Windows):
# PHYSICS_ENGINE_PLUGIN_PATH=C:\Program Files\PhysicsPlugins;C:\Users\User\Plugins
#
# Leave unset to use built-in plugins only (default behavior)
PHYSICS_ENGINE_PLUGIN_PATH=

# Warning Controls (New in v0.1.1)
# ----------------------------------
# ⚠️ IMPORTANT: These are REFERENCE VALUES ONLY - not active configuration!
# Warning controls must be set via the plugin API in your code, not via environment variables.
# These values document the defaults and are provided for reference.
#
# Configure in code using:
#   let mut gravity = GravityPlugin::new(G);
#   gravity.set_max_expected_force(1e12);
#   gravity.set_warn_on_high_forces(false);
#
# See docs/plugins.md for complete API reference.

# Maximum expected force magnitude (Newtons) - REFERENCE ONLY
# Default in code: 1e10 (10 billion Newtons)
# Example for solar system simulations: 1e23
# PHYSICS_ENGINE_MAX_FORCE=1e10

# Enable/disable high-force warnings - REFERENCE ONLY
# Set to "0" to suppress warnings in known high-force scenarios. Use "1" to enable.
# Default in code: 1 (warnings enabled)
# PHYSICS_ENGINE_WARN_HIGH_FORCES=1

# Gravitational softening factor (meters) - REFERENCE ONLY
# Default in code: 1e3 (1 km)
# PHYSICS_ENGINE_SOFTENING=1e3

# Logging Configuration (Future)
# --------------------------------
# Future versions will support configurable logging levels
# PHYSICS_ENGINE_LOG_LEVEL=info
# PHYSICS_ENGINE_LOG_FILE=/var/log/physics-engine.log

# Performance Tuning (Future)
# -----------------------------
# Future versions will support runtime performance configuration
# PHYSICS_ENGINE_THREAD_COUNT=auto
# PHYSICS_ENGINE_BATCH_SIZE=1000

# Diagnostics (New in v0.1.1)
# -----------------------------
# ⚠️ IMPORTANT: These are REFERENCE VALUES ONLY - not active configuration!
# Diagnostic flags are passed via command-line arguments to example programs.
# Use the --diagnostics flag when running examples:
#   cargo run --release --example solar_system -- --diagnostics
#   cargo run --release --example particle_collision -- --diagnostics
#
# See docs/DIAGNOSTICS_README.md for usage details.
#
# Future versions may support environment-based diagnostic configuration.
# These reference values document planned functionality:
# PHYSICS_ENGINE_DIAGNOSTICS=0
# PHYSICS_ENGINE_DIAG_INTERVAL=10
